# Chat Cards

This is a simple Chat Cards project that displays cards of different persons' chats, showing **from**, **to**, **message**, and **date & time** for each chat.  
Built to practice CRUD operations using MongoDB with REST API requests (GET, POST, PATCH/PUT, DELETE).
